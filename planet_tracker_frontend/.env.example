# Frontend environment variables (copy to .env and adjust as needed)

# Backend base URL (optional; if not set the app derives :3001 from current origin)
REACT_APP_API_BASE_URL=https://localhost:3001

# NASA API key (optional but recommended for better APOD fallback quality)
REACT_APP_NASA_API_KEY=DEMO_KEY
